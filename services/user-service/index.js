// --- 1. Імпорти (Imports) ---
// Імпортуємо фреймворк Express для створення веб-сервера.
const express = require('express');
// Імпортуємо MongoClient з офіційного драйвера MongoDB для підключення до бази даних.
const { MongoClient } = require('mongodb');

// --- 2. Налаштування (Configuration) ---
// Створюємо екземпляр додатку Express.
const app = express();
// Визначаємо порт, на якому буде працювати наш сервіс.
const PORT = 3002;

// URL для підключення до нашої бази даних MongoDB.
// "mongodb://localhost:27017" - це стандартна адреса, якщо MongoDB запущена локально.
// У Docker-середовищі тут буде ім'я контейнера, наприклад "mongodb://mongodb:27017".
const MONGO_URL = 'mongodb://user-db:27017';
// Назва нашої бази даних.
const DB_NAME = 'user_db';

// --- 3. Middleware ---
// Додаємо вбудований в Express middleware для розбору JSON-тіл запитів.
// Це дозволить нам легко отримувати дані з POST-запитів, наприклад, від auth-service.
app.use(express.json());

// --- 4. Підключення до бази даних та запуск сервера ---
// Оголошуємо змінну для зберігання об'єкту бази даних, щоб він був доступний у всьому додатку.
let db;

// Функція для підключення до MongoDB і запуску сервера.
async function startServer() {
  try {
    // Створюємо нового клієнта для підключення до MongoDB.
    const client = new MongoClient(MONGO_URL);
    // Підключаємося до сервера MongoDB.
    await client.connect();
    console.log('✅ Успішно підключено до MongoDB');

    // Отримуємо доступ до нашої бази даних. Якщо її немає, MongoDB створить її автоматично.
    db = client.db(DB_NAME);

    // Запускаємо сервер Express, щоб він почав приймати запити.
    // Cервер стартує тільки ПІСЛЯ успішного підключення до БД.
    app.listen(PORT, () => {
      console.log(`🚀 Сервіс користувачів запущено на порту ${PORT}`);
    });
  } catch (err) {
    // Якщо сталася помилка підключення до БД, виводимо її в консоль і завершуємо процес.
    console.error('❌ Помилка підключення до MongoDB:', err);
    process.exit(1);
  }
}

// --- 5. Ендпоінти (API Endpoints) ---

// Кореневий ендпоінт для перевірки працездатності сервісу.
app.get('/', (req, res) => {
  res.send('Hello from User Service! 👋');
});

/**
 * Ендпоінт для створення нового профілю користувача.
 * Очікується, що цей ендпоінт буде викликаний сервісом автентифікації (auth-service)
 * після реєстрації нового користувача.
 */
app.post('/users', async (req, res) => {
  // Перевіряємо, чи підключена база даних.
  if (!db) {
    return res.status(500).json({ message: 'Помилка: База даних не підключена.' });
  }

  try {
    // Отримуємо дані з тіла запиту.
    const { authId, phone, name, balance } = req.body;

    // --- Валідація вхідних даних ---
    // Перевіряємо, чи передані обов'язкові поля.
    if (!authId || !phone || !name) {
      console.log('Помилка валідації: відсутні обов\'язкові поля', req.body);
      return res.status(400).json({ message: 'Поля "authId", "phone" та "name" є обов\'язковими.' });
    }

    // --- Підготовка даних для збереження ---
    // Створюємо об'єкт користувача для збереження в БД.
    const newUser = {
      authId: authId, // Зберігаємо authId з auth-service як auth_id для чіткості.
      name: name,
      phone: phone,
      // Перевіряємо, чи передано баланс. Якщо ні, встановлюємо 0 за замовчуванням.
      balance: balance !== undefined ? balance : 0,
      createdAt: new Date(), // Додаємо дату створення для аудиту.
    };

    console.log('Спроба створити нового користувача:', newUser);

    // --- Збереження в БД ---
    // Отримуємо доступ до колекції 'users'. Якщо її немає, вона буде створена автоматично.
    const usersCollection = db.collection('users');
    // Вставляємо новий документ (користувача) в колекцію.
    const result = await usersCollection.insertOne(newUser);

    console.log(`✅ Користувача успішно створено з ID: ${result.insertedId}`);

    // --- Відповідь клієнту ---
    // Відправляємо відповідь зі статусом 201 (Created) і даними створеного користувача.
    res.status(201).json(newUser);

  } catch (error) {
    // Обробка можливих помилок під час запису в БД.
    console.error('Помилка при створенні користувача:', error);
    res.status(500).json({ message: 'Внутрішня помилка сервера при створенні користувача.' });
  }
});

// --- 6. Запуск додатку ---
// Викликаємо нашу асинхронну функцію для старту всього процесу.
startServer();